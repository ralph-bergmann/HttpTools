name: http_client_interceptor
description: >-
  A Dart package for intercepting and modifying HTTP requests and responses, 
  useful for logging, authentication, and error handling.
topics:
  - http
  - network
  - interceptor
  - middleware
  - error-handling
version: 1.0.1
repository: https://github.com/ralph-bergmann/HttpTools
issue_tracker: https://github.com/ralph-bergmann/HttpTools/issues

environment:
  sdk: ^3.10.8
resolution: workspace

dependencies:
  collection: ^1.19.1
  http: ^1.6.0
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.1.0
  shelf: ^1.4.2
  shelf_test_handler: ^2.0.2
  test: ^1.29.0
